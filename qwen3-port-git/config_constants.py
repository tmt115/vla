# config_constants.py — generated by Phase 0, do not edit manually
# Target: Qwen/Qwen3-VL-7B-Instruct
# Architecture constants derived from transformers Qwen3VLConfig (7B variant).
# No local checkpoint available; values cross-referenced against published
# model card and NxDI qwen3_vl source (modeling_qwen3_vl.py).
# Vision constants confirmed against transformers.models.qwen3_vl.configuration_qwen3_vl.Qwen3VLVisionConfig defaults.

# ── Text decoder (language model) ─────────────────────────────────────────────
TEXT_HIDDEN_SIZE = 3584
TEXT_NUM_ATTENTION_HEADS = 28
TEXT_NUM_KEY_VALUE_HEADS = 4
TEXT_NUM_HIDDEN_LAYERS = 28
TEXT_INTERMEDIATE_SIZE = 18944
TEXT_HEAD_DIM = 128               # hidden_size // num_attention_heads = 3584 // 28 = 128
TEXT_VOCAB_SIZE = 151936
TEXT_RMS_NORM_EPS = 1e-06
TEXT_ROPE_THETA = 1000000.0
TEXT_MAX_POSITION_EMBEDDINGS = 32768
TEXT_ROPE_SCALING = {"rope_type": "default", "mrope_section": [24, 20, 20]}
TEXT_ROPE_MROPE_SECTION = [24, 20, 20]

# ── Vision encoder (ViT) ───────────────────────────────────────────────────────
VIS_DEPTH = 27                    # number of transformer blocks
VIS_HIDDEN_SIZE = 1152            # ViT hidden dimension
VIS_INTERMEDIATE_SIZE = 4304      # ViT MLP intermediate dimension
VIS_NUM_HEADS = 16
VIS_IN_CHANNELS = 3
VIS_PATCH_SIZE = 16               # spatial patch size (pixels)
VIS_TEMPORAL_PATCH_SIZE = 2       # temporal patch size (frames)
VIS_SPATIAL_MERGE_SIZE = 2        # patch merger downscale factor
VIS_OUT_HIDDEN_SIZE = 3584        # projector output = text hidden size
VIS_NUM_POSITION_EMBEDDINGS = 2304  # sqrt(2304) = 48 → 48×48 grid
VIS_HIDDEN_ACT = "gelu_pytorch_tanh"
VIS_HEAD_DIM = VIS_HIDDEN_SIZE // VIS_NUM_HEADS   # 72
VIS_DEEPSTACK_VISUAL_INDEXES = [8, 16, 24]        # intermediate layer capture indices

# ── Special token ids ─────────────────────────────────────────────────────────
IMAGE_TOKEN_ID = 151655
VIDEO_TOKEN_ID = 151656
VISION_START_TOKEN_ID = 151652
VISION_END_TOKEN_ID = 151653

# ── Vision bucketing (seq len = number of flattened image patches) ─────────────
# Each bucket is a vision sequence length (patches, before spatial merge).
# Realistic image sizes after HF preprocessing (shortest/longest edge clamp):
#   224×224  → T=1, H=14, W=14 → 196 patches
#   448×448  → T=1, H=28, W=28 → 784 patches
#   896×896  → T=1, H=56, W=56 → 3136 patches
#   1792×1792 → T=1, H=112, W=112 → 12544 patches
# Merge by spatial_merge_size=2 → divide by 4 to get text-side tokens.
VISION_BUCKETS = [784, 3136, 12544]   # latency-priority: 3 buckets
VISION_SEQ_LEN = 12544               # largest bucket

# ── Inference / Neuron config ─────────────────────────────────────────────────
TP_DEGREE = 8
MAX_BATCH_SIZE = 1
TEXT_SEQ_LEN = 8192          # context window for tracing (covers vision + text)
TEXT_MAX_CONTEXT_LENGTH = 8192
TORCH_DTYPE = "bfloat16"

# ── Derived constants ─────────────────────────────────────────────────────────
VIS_PATCHES_PER_PIXEL_SEQ = VIS_IN_CHANNELS * VIS_TEMPORAL_PATCH_SIZE * VIS_PATCH_SIZE * VIS_PATCH_SIZE
# = 3 * 2 * 16 * 16 = 1536
